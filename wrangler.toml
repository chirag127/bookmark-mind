name = "bookmark-mind"
pages_build_output_dir = "./public"
compatibility_date = "2026-06-17"

# Static marketing site + privacy policy. Built by `node scripts/build-site.mjs`.
# Deployed via .github/workflows/deploy.yml → `wrangler pages deploy public`.
# Custom domain bound via dashboard:
#   bookmark-mind.oriz.in -> bookmark-mind
